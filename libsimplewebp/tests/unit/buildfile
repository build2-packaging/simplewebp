import libs = libsimplewebp%lib{simplewebp}

# Source-tree test data. Listed on the exe so update-for-test
# sees the files. The testscript links data/ into $~. data/ is
# a real directory of per-entry upstream symlinks (build2#549).
#
./: exe{driver}: c{driver} $libs testscript
exe{driver}: file{data/**}: include = adhoc
